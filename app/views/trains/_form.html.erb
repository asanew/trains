<%= form_with(model: train, local: true) do |form| %>
  <%= render 'common/error', resource: train %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number, id: :train_number %>
    <%= form.label :route %>
    <%= form.collection_select :route_id, Route.all, :id, :name %>
    <%= form.label :current_station %>
    <%= form.collection_select :current_station_id, RailwayStation.all, :id, :title %>
    <%= form.check_box :wagons_order, {}, 'true', 'false' %> Wagons order from last
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
