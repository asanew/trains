<% unless train.wagons.empty? %>
  <table>
    <thead>
    <tr>
      <th>Wagon type</th>
      <th>Number</th>
      <th>Top seats</th>
      <th>Bottom seats</th>
    </tr>
    </thead>

    <tbody>
    <% wagons_info = train.get_wagons_data %>
    <% WagonType.all.each do |wagon_type| %>
      <tr>
        <td><%= wagon_type.name %></td>
        <td><%= wagons_info[wagon_type][:quantity] %></td>
        <td><%= wagons_info[wagon_type][:top_seats] %></td>
        <td><%= wagons_info[wagon_type][:bottom_seats] %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
<% else %>
  Train has no wagons
<% end %>
